#include<stdio.h>
float main()
{
float a,b;
printf ("Key in the first number a:");
scanf("%f",&a);
printf("Key in the second number b:");
scanf("%f",&b);
float f=a-b;
printf("The input values of a&b %f \n",f);
} 

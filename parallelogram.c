#include<stdio.h>
float main()
{
float b,h,area;
printf("Input the value of b: ");
scanf("%f", &b);
printf("Input the value of h: ");
scanf("%f", &h);
area=b*h;
printf("Area of Parallelogram is: %f \n",area);
}
